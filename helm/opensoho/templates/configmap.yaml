apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "opensoho.fullname" . }}-config
  labels:
    {{- include "opensoho.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.env }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}

